import type { Theme } from "../types";

export const mykoDefault: Theme = {
  id: "myko-default",
  name: "Myko Default",
  description: "The default Myko look — clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};
